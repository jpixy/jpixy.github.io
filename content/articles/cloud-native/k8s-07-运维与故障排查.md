+++
title = "07.Kubernetes运维与故障排查"
description = "K8s日常运维、监控告警、日志管理与故障排查实战"
date = 2025-01-16
[taxonomies]
tags = ["kubernetes", "container", "devops", "troubleshooting", "monitoring"]
+++

# Kubernetes运维与故障排查

## 一、日常运维

### 1.1 集群健康检查

```bash
# 检查节点状态
kubectl get nodes -o wide
kubectl describe node <node-name>

# 检查组件状态
kubectl get componentstatuses  # 已废弃但仍可用
kubectl get --raw='/readyz?verbose'
kubectl get --raw='/healthz?verbose'

# 检查系统Pod
kubectl get pods -n kube-system

# 检查API Server
kubectl cluster-info
kubectl version

# 检查etcd
ETCDCTL_API=3 etcdctl endpoint health \
  --endpoints=https://127.0.0.1:2379 \
  --cacert=/etc/kubernetes/pki/etcd/ca.crt \
  --cert=/etc/kubernetes/pki/etcd/server.crt \
  --key=/etc/kubernetes/pki/etcd/server.key
```

### 1.2 资源管理

```bash
# 查看资源使用情况
kubectl top nodes
kubectl top pods -A

# 查看资源配额
kubectl get resourcequota -A
kubectl describe resourcequota <name> -n <namespace>

# 查看限制范围
kubectl get limitrange -A

# 资源清理
kubectl delete pod --field-selector=status.phase==Succeeded -A
kubectl delete pod --field-selector=status.phase==Failed -A
```

**ResourceQuota配置**：

```yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: production
spec:
  hard:
    requests.cpu: "100"
    requests.memory: 100Gi
    limits.cpu: "200"
    limits.memory: 200Gi
    pods: "100"
    persistentvolumeclaims: "50"
    services.loadbalancers: "5"
```

### 1.3 节点管理

```bash
# 节点维护（排空工作负载）
kubectl cordon <node>  # 标记不可调度
kubectl drain <node> --ignore-daemonsets --delete-emptydir-data
# 执行维护...
kubectl uncordon <node>  # 恢复调度

# 添加/删除标签
kubectl label nodes <node> disktype=ssd
kubectl label nodes <node> disktype-

# 添加/删除污点
kubectl taint nodes <node> key=value:NoSchedule
kubectl taint nodes <node> key-
```

---

## 相关文章

- [上一篇：Kubernetes安全与RBAC](/articles/cloud-native/k8s-06-安全与RBAC/)
- [下一篇：Helm包管理详解](/articles/cloud-native/k8s-08-Helm包管理/)
